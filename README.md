# Cashier and inventory is for pet shop
before run the app make sure that you create the database using db file "PetShopDB" so the app could run without problem
this app is developed using ssms20 with windows authentication for the database.
in addition this app is developed using nuget materialskin 2.0 and windows reporting(rdlc)
